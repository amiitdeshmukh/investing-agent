# Public backend origins only. Values in this file are safe to expose to the
# browser. Never add Groww, database, model, or session secrets here.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws/live
